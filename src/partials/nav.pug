

mixin nav(nav,topDir)
    nav.nav.content-container
        ul.nav-item-list.flex
            li.nav-item.small-caps
                a(href=topDir) renoj
            li.nav-item
                ul.nav-item-list.flex 
                    li.nav-item.small-caps.mr-xl
                        a(href=topDir) work
                    li.nav-item.small-caps.right
                        a(href=topDir) about

            

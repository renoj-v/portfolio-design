mixin img(src, alt)
    .img-container
        img(src=src alt=alt)

mixin youtube(key, alt)
    .img-container
        .video-wrapper
            iframe(src="https://www.youtube.com/embed/"+key alt=alt allowfullscreen  frameborder="0")

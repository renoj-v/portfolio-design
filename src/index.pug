extends ./layout
        
block content
    - const subpages = self.htmlWebpackPlugin.options.pages.slice(1)
    
    .super-container
        .container.clearfix
            each page in subpages
                a.portfolio-link(href=page.filename style="opacity:0")
                    .portfolio-item
                        .center.rotate-high
                            .img-container
                                img(src=images+"Screen1.png")            
                        .center-text 
                        h3.header=page.title
    
